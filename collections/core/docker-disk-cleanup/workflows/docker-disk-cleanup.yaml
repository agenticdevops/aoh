apiVersion: openagentix.io/v1alpha1
kind: Workflow
metadata:
  name: docker-disk-cleanup
  description: Diagnose local Docker disk pressure and produce safe cleanup recommendations.
spec:
  skills:
    - docker-disk-cleanup
  agentRole: ops-triage-lead
  modelProfile: local-worker
  runtimeRequirements:
    - docker-readonly
  evals:
    - docker-disk-cleanup-basic
