apiVersion: openagentix.io/v1alpha2
kind: ModelProfile
metadata:
  name: local-worker
spec:
  intent: Execute known diagnostic operations with a local or low-cost worker model.
  fallback: frontier-unblocker
